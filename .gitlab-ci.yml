image: "python:3.7"
services:
  - mysql:5.7

cache:
  paths:
    - .cache/pip
    - venv/

stages:
  - checking

before_script:
  - ./build.sh

Build:
  stage: Checking
  script:
    - python3 manage.py migrate
  tags:
    - shell
